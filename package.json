{
  "name": "@graphql-otel/monorepo",
  "version": "0.0.0-alpha.2",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:ui": "turbo dev --filter \"@graphql-debugger/ui\"",
    "dev:proxy": "turbo dev --filter \"@graphql-debugger/collector-proxy\"",
    "debugger": "turbo dev --filter \"graphql-debugger\"",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^1.10.7",
    "typescript": "^5.1.6"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@babel/*",
        "typescript"
      ]
    }
  },
  "prettier": {
    "printWidth": 100,
    "tabWidth": 2,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}