# GraphQL Debugger In Docker

- https://www.graphql-debugger.com/

To pull the docker image, run the following command:

```bash
docker pull graphqldebugger/graphqldebugger
```

To run the docker image, run the following command:

```bash
docker run --name graphql-debugger -p 16686:16686 -p 4318:4318 graphqldebugger/graphqldebugger
```

## Docker Compose

To use GraphQL Debugger inside docker compose, add the following to your `docker-compose.yml` file:

```yaml
graphql-debugger:
  image: graphqldebugger/graphqldebugger
  ports:
    - 16686:16686
    - 4318:4318
```
